<%= render DmUniboCommon::ModalComponent.new() do %>
  <div class="list-group list-group-flush">
    <% Category.find_each do |c| %>
      <%= button_to c, set_category_good_path(@good, category_id: c.id), class: "list-group-item list-group-item-action m-0" %>
    <% end %>
  </div>
<% end %>

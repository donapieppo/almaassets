<h1><span class="mr-5"><%= category_icon(@category, size: 40) if @category %></span> <%= @title %></h1>

<div class="list-group">
  <% @goods.each do |good| %>  
    <%= render good, no_icon: @no_icon, search_string: @search_string %>  
  <% end %>
</div>


<h1>
  <span class="mr-5"><%= category_icon(@category, size: 40) if @category %></span> 
  <%= @title %>
</h1>

<div>
  <% @goods.each do |good| %>  
    <%= render Good::CardComponent.new(good, current_user, no_icon: @no_icon, search_string: @search_string) %>  
  <% end %>
</div>


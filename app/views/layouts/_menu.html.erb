<%= dm_menu do %>  
  <%= dm_header %>
  <%= dm_nav do  %>  
    <li><%= link_to big_icon('home', text: 'Home'), root_path %></li>
    <% if current_user.is_admin? %>  
      <li><%= link_to big_icon('list', text: 'list'), goods_path(all: true) %></li>
    <% end %>
  <% end %>
<% end %>


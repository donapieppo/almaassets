<%= dm_form_for @server do |server| %>  
  <%= server.input :name %>  
  <%= server.input :url %>  
  <%= server.input :hardware, input_html: { rows: 8 } %>  
  <%= server.input :software, input_html: { rows: 8 } %>  
  <%= server.submit %>  
<% end %>

